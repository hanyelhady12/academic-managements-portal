# نسخة محلية من نظام إدارة أكاديمي
# Local Version of Academic Management System

## 🚀 كيفية التشغيل - How to Run

### المتطلبات - Requirements:
1. **Node.js** (مثبت على جهازك)
2. **Bun** (مدير الحزم المفضل)
   - تحميل: https://bun.sh
   - التثبيت: `curl -fsSL https://bun.sh/install | bash`

3. **اتصال بالإنترنت** (للاتصال بقاعدة البيانات Neon)

### خطوات التشغيل - Steps:

1. **تحميل المشروع**
   - أنشئ مجلد جديد على سطح المكتب
   - قم بتحميل جميع ملفات المشروع من GitHub:
     https://github.com/hanyelhady12/academic-managements-portal

2. **تثبيت الحزم**
   ```bash
   cd /path/to/project/folder
   bun install
   ```

3. **إنشاء ملف .env**
   أنشئ ملف اسمه `.env` بجانب ملفات المشروع واكتب فيه:
   ```bash
   DATABASE_URL="postgresql://neondb_owner:npg_aNTE8eCxRB1b@ep-steep-pond-a16wpsaq-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
   NEXTAUTH_URL="http://localhost:3000"
   NEXTAUTH_SECRET="academic-system-secure-key-2024"
   ```

4. **تشغيل البرنامج**
   ```bash
   bun run dev
   ```

5. **فتح المتصفح**
   افتح المتصفح على هذا العنوان:
   ```
   http://localhost:3000
   ```

---

## 📋 بيانات الدخول - Login Credentials

**البريد الإلكتروني:** `admin@academic.com`
**كلمة المرور:** `Secure@Pass123!`

---

## 📊 ميزات النسخة المحلية - Features

### ✅ المزايا:
- **العمل بدون إنترنت متصل** (يحتاج إنترنت فقط لقاعدة البيانات)
- **السرعة العالية** (تطبيق محلي أسرع)
- **عدم التداخل** (كل مستخدم له نسخته الخاصة)
- **التحديث السهل** (تحديث المشروع من GitHub وتثبيته مجدداً)

### 🔄 المزامنة - Sync with Online:
- أي بيانات تُضاف في النسخة المحلية تُحفظ في قاعدة البيانات المركزية
- عند فتح الموقع على الإنترنت، ستجد نفس البيانات

### ⚠️ الملاحظات الهامة:

1. **قاعدة البيانات المشتركة**
   - النسخة المحلية والنسخة الإلكترونية تستخدم نفس قاعدة البيانات
   - أي تغييرات تحدث في مكان تظهر في المكان الآخر

2. **اتصال بالإنترنت**
   - النسخة المحلية تحتاج اتصالاً بالإنترنت لقاعدة البيانات Neon
   - بدون إنترنت لن يعمل البرنامج

3. **التحديثات**
   - لتحديث البرنامج، قم بتحميل النسخة الجديدة من GitHub:
     https://github.com/hanyelhady12/academic-managements-portal

---

## 🛠️ حل المشاكل - Troubleshooting

### المشكلة: لا يعمل البرنامج
**الحل:**
```bash
# تأكد من تثبيت الحزم
bun install

# تأكد من إنشاء ملف .env
ls -la .env

# تأكد من صحة قاعدة البيانات
bun run db:push
```

### المشكلة: خطأ في الدخول
**الحل:**
1. تحقق من صحة بيانات الدخول
2. قم بتغيير كلمة المرور:
   ```bash
   DATABASE_URL="postgresql://neondb_owner:npg_aNTE8eCxRB1b@ep-steep-pond-a16wpsaq-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require" bunx -e "
   import { PrismaClient } from '@prisma/client'
   const db = new PrismaClient()
   await db.user.update({
     where: { email: 'admin@academic.com' },
     data: { password: 'NEW_PASSWORD' }
   })
   console.log('تم تغيير كلمة المرور')
   await db.\$disconnect()
   "
   ```

---

## 📞 الدعم الفني - Support

إذا واجهت أي مشكلة:
- البرنامج على الإنترنت: https://academic-managements-portal.vercel.app
- الدعم الفني: تواصل مع مطور البرنامج

---

**آخر تحديث:** January 2026
**الإصدار:** 1.0.0
